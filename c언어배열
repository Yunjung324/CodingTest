/* //555
#include <stdio.h>

int main(){
    char arr[10];
    for(int i=0; i<10; i++){
        scanf(" %c", &arr[i]);
    }
    for(int i=0; i<10; i++){
        printf("%c", arr[i]);
    }
}
*/

/* //556
#include <stdio.h>

int main(){
    int arr[11];
    for(int i =1; i<=10; i++)
        arr[i] = i;
    for(int i =1; i<=10; i++)
        printf("%d ",arr[i]);
}
*/

/* //557
#include <stdio.h>

int main(){
    char arr[11];
    for(int i = 1; i<11; i++)
        scanf(" %c", &arr[i]);
    printf("%c %c %c", arr[1], arr[4], arr[7]);
}*/

/*
//558
#include <stdio.h>

int main(){
    int i, j, l;
    int arr[100];
    for(i=0; i<100; i++){
        scanf("%d", &arr[i]);
        if(arr[i] == 0){
            l = i-1;
            break;
        }
    }
    for(j=l; j>=0; j--) printf("%d ", arr[j]);
}
*/

/* //1291
#include <stdio.h>

int main(){
    int s, e, i, j;
    while(1){
        scanf("%d %d", &s, &e);
        if((s>=2 && e>=2)&&(s<=9 && e<=9)) break;
        else printf("INPUT ERROR!\n");
    }
    if(s<e){
        for(i = 1; i<10; i++){
            for(j = s; j<=e; j++){
                printf("%d * %d = %2d   ", j, i, j*i);
            }
            printf("\n");
        }
    }
    else{
        for(i = 1; i<10; i++){
            for(j=s; j>=e; j--){
                printf("%d * %d = %2d   ", j, i, j*i);
            }
            printf("\n");
        }
    }
    return 0;
}
*/

/* //1341
#include <stdio.h>

int main(){
    int s, e, i, j;
    scanf("%d %d", &s, &e);
    if(s<e){
        for(j = s; j <= e; j++){
            for(i = 1; i<10; i++){
                printf("%d * %d = %2d   ", j, i, j*i);
                if(i%3 == 0) printf("\n");
            }
            printf("\n");
        }
    }
    else{
        for(j=s; j>=e; j--){
            for(i=1; i<10; i++){
                printf("%d * %d = %2d   ", j, i, j*i);
                if(i %3 == 0) printf("\n");
            }
            printf("\n");
        }
    }
    return 0;
}
*/
